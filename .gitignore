# API keys and credentials
agent/auth.json

# Session history (per-machine, changes frequently)
agent/sessions/

# Precompiled binaries (machine-specific)
agent/bin/

# Extension/npm dependencies (installed on each machine)
agent/node_modules/
agent/npm/

# Compiled extensions (auto-generated)
agent/extensions/**/*.js
